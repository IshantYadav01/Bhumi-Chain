# ---------------------------------------------------------------------------
# crypto-config.yaml — 3 Provincial Full Nodes (test)
# 77 Malpots, buyers, sellers, officials = lite nodes (connect via SDK)
# Scale to 11 provinces for production.
# ---------------------------------------------------------------------------

OrdererOrgs:
  - Name: Orderer
    Domain: example.com
    EnableNodeOUs: true
    Specs:
      - Hostname: orderer

PeerOrgs:
  - Name: Province1
    Domain: province1.example.com
    EnableNodeOUs: true
    Template: { Count: 1 }
    Users: { Count: 2 }

  - Name: Province2
    Domain: province2.example.com
    EnableNodeOUs: true
    Template: { Count: 1 }
    Users: { Count: 2 }

  - Name: Province3
    Domain: province3.example.com
    EnableNodeOUs: true
    Template: { Count: 1 }
    Users: { Count: 2 }
